spring:
  application:
    name: ratelimiter
  data:
    redis:
      host: localhost
      port: 6379
# Redis Cluster Configuration (Pointing to our master nodes)
#      cluster:
#        nodes: localhost:7000,localhost:7001,localhost:7002

#  Instead of launching just one Redis container, we need to spin up multiple Redis instances that talk to each other.
#  A standard production Redis Cluster requires at least 3 master nodes (to split the data across 3 separate servers).

#docker command
#  docker run -p 7000:7000 -p 7001:7001 -p 7002:7002 -p 7003:7003 -p 7004:7004 -p 7005:7005 --name redis-cluster-bouncer -d grokzen/redis-cluster:latest